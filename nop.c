#include "monty.h"
/**
  *f_nop- does nothing (NOP - No Operation)
  *@head:pointer to the stack head
  *@counter: line number for error reporting
  *Return: no return value
 */
void f_nop(stack_t **head, unsigned int counter)
{
	(void) counter;
	(void) head;
}
